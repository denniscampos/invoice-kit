# Current work

**Status:** nothing in progress

No feature, fix, or rollback is being built right now. This file is the spec for
the one thing in flight, and it is loaded into every session automatically, so it
stays a stub until the next item starts.

Start the next one with:

- `/feature` - spec the next unchecked item in `blueprint/build-plan.md`, or
  `/feature 4` / `/feature "template selection"` to pick a specific one
- `/fix` - document an ad-hoc bug or change that is not a build-plan item
- `/rollback` - plan a safe reversal of a completed feature

Completed work lives under `blueprint/history/`.
