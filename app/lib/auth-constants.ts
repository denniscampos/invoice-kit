/* Shared by the auth server, the message that explains a refusal, and the label
   on the form. One number in one place: a form that promises six characters
   while the server demands eight is a small betrayal that is easy to ship. */
export const MIN_PASSWORD_LENGTH = 8;
